"use strict";(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[355],{758:(e,t,n)=>{n.d(t,{I7:()=>G,dg:()=>I,r:()=>U,v0:()=>F,v6:()=>P});var a=n(6521),r=n(4576),i=n(6820),o=n(9245),l=n(6234),s=n(970);let c=null,u=null,v=!1,h=null,d=0,p=0,m=!1,f=0,g=0,w=0,y=0,x=0,M=null,b=null,k=0,T=new Set;function A(e,t){let n=arguments.length>2&&void 0!==arguments[2]?arguments[2]:1,a=Math.floor(4*t.sampleRate),r=t.createBuffer(n,a,t.sampleRate);for(let t=0;t<n;t++)!function(e,t){if("pink"===t){let t=0,n=0,a=0,r=0,i=0,o=0,l=0;for(let s=0;s<e.length;s++){let c=2*Math.random()-1;t=.99886*t+.0555179*c,n=.99332*n+.0750759*c,a=.969*a+.153852*c,r=.8665*r+.3104856*c,i=.55*i+.5329522*c,o=-.7616*o-.016898*c,e[s]=(t+n+a+r+i+o+l+.5362*c)*.11,l=.115926*c}}else{let t=0;for(let n=0;n<e.length;n++)t=(t+.02*(2*Math.random()-1))/1.02,e[n]=3.5*t}}(r.getChannelData(t),e);return r}function S(e,t,n){let a=arguments.length>3&&void 0!==arguments[3]&&arguments[3],r=Math.floor(e.sampleRate*t),i=e.createBuffer(2,r,e.sampleRate);for(let e=0;e<2;e++){let t=i.getChannelData(e),o=0;for(let e=0;e<r;e++){let i=2*Math.random()-1;o+=(i-o)*.16,t[e]=(a?2.4*o:i)*Math.pow(1-e/r,n)}}return i}function q(e,t,n,a,r){let i=arguments.length>5&&void 0!==arguments[5]?arguments[5]:0;if(t.positionX)if(i>0){let o=e.currentTime;t.positionX.setTargetAtTime(n,o,i),t.positionY.setTargetAtTime(a,o,i),t.positionZ.setTargetAtTime(r,o,i)}else t.positionX.value=n,t.positionY.value=a,t.positionZ.value=r;else t.setPosition(n,a,r)}function C(e,t,n,a){let r=e.createPanner();return r.panningModel="HRTF",r.distanceModel="inverse",r.refDistance=1,r.rolloffFactor=.6,q(e,r,t,n,a),r}let D=[41.2,41.2,32.7,36.71];function R(e){if(requestAnimationFrame(R),!c||!u||"running"!==c.state)return;let t=Math.min((e-f)/1e3,.1);f=e;let n=e/1e3,v=(0,a.UW)(),h=Math.min(v.speed/45,1);p+=(h-p)*Math.min(1,t/1.8);let M=Math.min(Math.max(0,h-1.15*p),.8);M>d?d+=(M-d)*Math.min(1,t/.35):d=Math.max(d*Math.exp(-t/2.2),M);let b=(0,r.X)(),k=Math.max(Math.min(b.stage,4)-2*Math.max(0,b.stage-4),0),T=Math.min((Math.pow(2,.5*k)-1)/3,1)*b.amt,A=((e,t,n)=>{let a=Math.min(Math.max((n-e)/(t-e),0),1);return a*a*(3-2*a)})(3.1,3.95,k)*b.amt,S=(0,i.rf)().amt,C=(0,l.DJ)(),G=Math.min(Math.abs(C)/4.5,1),I=+!document.hidden;g+=((0,o.rP)().target-g)*Math.min(1,t/1.2),w*=Math.exp(-t/1.8);let U=Math.abs(S-x)/Math.max(t,.001);x=S,y=Math.max(y*Math.exp(-t/1.1),Math.min(1.6*U,1));let P=u,F=c,B=F.currentTime,z=function(e,t){let n=arguments.length>2&&void 0!==arguments[2]?arguments[2]:.15;return e.setTargetAtTime(t,B,n)},L=1+(.3+.15*g)*Math.sin(n*(.45-.17*g));z(P.floorGain.gain,(.006*L+.02*S+.008*T)*I,.25),z(P.floorFilter.frequency,480+500*S-220*g,.3),z(P.brownGain.gain,(.022+.006*g+.095*T+.09*A+.05*S+.04*d)*I,.25),z(P.brownFilter.frequency,120-20*g+260*T+200*A+140*S+60*d,.25),z(P.cursorGain.gain,.05*d*I,.2),z(P.cursorFilter.frequency,380-80*g+450*d,.3);let E=Math.max(window.innerWidth/2,1),O=Math.max(window.innerHeight/2,1),Y=Math.max(-3.5,Math.min(3.5,v.x/E*3.5)),V=Math.max(-1.8,Math.min(1.8,v.y/O*1.8));q(F,P.cursorPan,Y,V,-1.2,.08),z(P.scrollGain.gain,.032*G*I,.18),q(F,P.scrollPan,0,Math.max(-2.4,Math.min(2.4,C/4.5*2.4)),-1.6,.12),z(P.wanderGain.gain,(.0035+.004*T+.006*S)*I,.3),q(F,P.wanderPan,2.8*Math.sin(.11*n),1.6*Math.sin(.073*n+1.7),-1+2.2*Math.cos(.089*n),.2);let H=(.0025+.006*g+.05*w+.04*y+.045*A)*I;for(let e=0;e<P.crackleGains.length;e++){let t=.35+.65*Math.max(0,Math.sin(n*(.9-.22*e)+2.1*e)*Math.sin(n*(.53+.17*e)+4.2*e));z(P.crackleGains[e].gain,H*t,.25)}let N=(0,s.lU)();(0,s.RT)(Math.exp(-t/.75));let Q=(.2*N+.05*y+.05*T+.11*A)*I;for(let e=0;e<P.electricGains.length;e++){let t=.4+.6*Math.max(0,Math.sin(n*(4.2+1.7*e)+2.6*e)*Math.sin(n*(2.9-.8*e)+5.1*e));z(P.electricGains[e].gain,Q*t,.05)}let W=Math.max(T,.85*S);z(P.subGain.gain,(.006+.004*g+.02*d+(.11+.02*g)*W*W+.1*A)*I,.3),z(P.wobbleLfo.frequency,(.9+2.4*W+1.5*A)*(1-.15*g),.3),z(P.wobbleFilter.frequency,70+130*W+110*A,.3),z(P.wobbleFreqDepth.gain,18+90*W+70*A,.3),z(P.wobbleAmpDepth.gain,.15+.45*W+.2*A,.3);let X=W>.32?D[Math.floor(n/2.4)%D.length]:41.2;z(P.sub.frequency,X,.18),z(P.sub2.frequency,2*X,.18),z(P.chordGain.gain,(Math.pow(S,1.4)*(.05+.012*g)+.009*A)*I,.45),z(P.chordFilter.frequency,320-90*g,.4);let _=n*(.45+.55*S);q(F,P.chordPan,2.4*Math.cos(_),.9,-(2.4*Math.sin(_))-.4,.1),z(P.shimmerGain.gain,(m?.01+.01*d:.0015*d)*I,.2),z(P.shimmerBand.frequency,2200-800*g,.4),z(P.verbDayOut.gain,.75*(1-g),.4),z(P.verbNightOut.gain,.85*g,.4)}function G(){if(!c||!M||!b||!v||"running"!==c.state)return;let e=performance.now();if(e-k<30)return;k=e;let t=c.createBufferSource();t.buffer=M,t.playbackRate.value=.85+.35*Math.random();let n=c.createGain();n.gain.value=.55+.45*Math.random(),t.connect(n),n.connect(b),t.start()}function I(){return v}function U(e){return T.add(e),()=>{T.delete(e)}}function P(e){v=e;try{localStorage.setItem("sound",e?"on":"off")}catch(e){}h&&(clearTimeout(h),h=null),w=1,e?(c||(u=function(e){let t=e.createGain();t.gain.value=0;let n=e.createDynamicsCompressor();n.threshold.value=-26,n.knee.value=24,n.ratio.value=4,n.attack.value=.008,n.release.value=.35,n.connect(t),t.connect(e.destination);let a=e.createGain();a.gain.value=1;let r=e.createConvolver();r.buffer=S(e,2.2,2.6);let i=e.createGain();i.gain.value=.75;let o=e.createConvolver();o.buffer=S(e,4.5,4.2,!0);let l=e.createGain();l.gain.value=0,a.connect(r),r.connect(i),i.connect(n),a.connect(o),o.connect(l),l.connect(n);let s=function(t){let n=arguments.length>1&&void 0!==arguments[1]?arguments[1]:0,a=e.createBufferSource();return a.buffer=t,a.loop=!0,a.start(0,n),a},c=A("pink",e,1),u=e.createBiquadFilter();u.type="lowpass",u.frequency.value=520,u.Q.value=.5;let v=e.createGain();v.gain.value=0,s(A("pink",e,2)).connect(u),u.connect(v),v.connect(n);let h=e.createBiquadFilter();h.type="lowpass",h.frequency.value=130,h.Q.value=.6;let d=e.createGain();d.gain.value=0,s(A("brown",e,2)).connect(h),h.connect(d),d.connect(n);let p=e.createBiquadFilter();p.type="lowpass",p.frequency.value=420,p.Q.value=.6;let m=e.createGain();m.gain.value=0;let f=C(e,0,0,-1.2);s(c,.7).connect(p),p.connect(m),m.connect(f),f.connect(n);let g=e.createBiquadFilter();g.type="bandpass",g.frequency.value=650,g.Q.value=.8;let w=e.createGain();w.gain.value=0;let y=C(e,0,0,-1.6);s(c,1.9).connect(g),g.connect(w),w.connect(y),y.connect(n);let x=e.createBiquadFilter();x.type="bandpass",x.frequency.value=2200,x.Q.value=1.4;let k=e.createGain();k.gain.value=0;let T=C(e,0,2.8,-.8);s(c,3.1).connect(x),x.connect(k),k.connect(T),T.connect(n),T.connect(a);let q=e.createBiquadFilter();q.type="bandpass",q.frequency.value=1e3,q.Q.value=.7;let D=e.createGain();D.gain.value=0;let R=C(e,2,.5,.5);s(c,2.5).connect(q),q.connect(D),D.connect(R),R.connect(n),R.connect(a);let G=function(e){let t=Math.floor(6*e.sampleRate),n=e.createBuffer(1,t,e.sampleRate),a=n.getChannelData(0),r=1.1/e.sampleRate,i=0;for(;i<t-64;)if(Math.random()<r){let e=(.25+.75*Math.random())*(.5>Math.random()?1:-1);a[i]+=e,a[i+1]+=-(.55*e),a[i+2]+=.22*e;let t=24+Math.floor(40*Math.random());for(let n=3;n<t;n++)a[i+n]+=.1*e*Math.pow(1-n/t,2)*(2*Math.random()-1);i+=t}else i++;return n}(e),I=[[.7,.3,-.5],[-.6,-.15,-.7],[.15,.85,.45]].map((t,a)=>{let r=e.createBiquadFilter();r.type="bandpass",r.frequency.value=1900+350*a,r.Q.value=.7;let i=e.createGain();i.gain.value=0;let o=C(e,t[0],t[1],t[2]);return s(G,2.1*a).connect(r),r.connect(i),i.connect(o),o.connect(n),i}),U=function(e){let t=Math.floor(6*e.sampleRate),n=e.createBuffer(1,t,e.sampleRate),a=n.getChannelData(0),r=.9/e.sampleRate,i=0;for(;i<t-2400;)if(Math.random()<r){let n=3+Math.floor(9*Math.random()),r=i;for(let e=0;e<n&&r<t-4;e++){let t=(.3+.7*Math.random())*(.5>Math.random()?-1:1)*(1-e/n*.5);a[r]+=t,a[r+1]+=-(.7*t),a[r+2]+=.3*t,r+=30+Math.floor(420*Math.random())}i=r+Math.floor(.02*e.sampleRate)}else i++;return n}(e),P=[[1.5,.9,-.9,3100],[-1.3,-.4,-1.2,2500]].map((t,r)=>{let[i,o,l,c]=t,u=e.createBiquadFilter();u.type="highpass",u.frequency.value=1300;let v=e.createBiquadFilter();v.type="bandpass",v.frequency.value=c,v.Q.value=1.1;let h=e.createGain();h.gain.value=0;let d=C(e,i,o,l);return s(U,2.7*r).connect(u),u.connect(v),v.connect(h),h.connect(d),d.connect(n),d.connect(a),h});M=function(e){let t=Math.floor(.045*e.sampleRate),n=e.createBuffer(1,t,e.sampleRate),a=n.getChannelData(0);for(let n=0;n<t;n++){let t=n/e.sampleRate;a[n]=(2*Math.random()-1)*Math.exp(-t/.0035)*.35+Math.sin(t*Math.PI*320)*Math.exp(-t/.013)*.6}return n}(e),(b=e.createGain()).gain.value=.05;let F=e.createBiquadFilter();F.type="lowpass",F.frequency.value=1100,F.Q.value=.5;let B=C(e,-.7,.15,-1.1);b.connect(F),F.connect(B),B.connect(n),B.connect(a);let z=e.createOscillator();z.type="sine",z.frequency.value=41.2;let L=e.createOscillator();L.type="triangle",L.frequency.value=82.4;let E=e.createGain();E.gain.value=.3;let O=e.createBiquadFilter();O.type="lowpass",O.frequency.value=80,O.Q.value=1.1;let Y=e.createGain();Y.gain.value=1;let V=e.createGain();V.gain.value=0,z.connect(O),L.connect(E),E.connect(O),O.connect(Y),Y.connect(V),V.connect(n);let H=e.createOscillator();H.type="sine",H.frequency.value=1.1;let N=e.createGain();N.gain.value=20;let Q=e.createGain();Q.gain.value=.2,H.connect(N),N.connect(O.frequency),H.connect(Q),Q.connect(Y.gain),z.start(),L.start(),H.start();let W=e.createBiquadFilter();W.type="lowpass",W.frequency.value=320,W.Q.value=.5;let X=e.createGain();X.gain.value=0,[82.41,98,123.47].forEach((t,n)=>{let a=e.createOscillator();a.type="sine",a.frequency.value=t,a.detune.value=0===n?0:1===n?3:-4,a.connect(W),a.start()});let _=C(e,2.2,.9,-.6);return W.connect(X),X.connect(_),_.connect(n),_.connect(a),{master:t,floorGain:v,floorFilter:u,brownGain:d,brownFilter:h,cursorGain:m,cursorFilter:p,cursorPan:f,scrollGain:w,scrollPan:y,shimmerGain:k,shimmerBand:x,wanderGain:D,wanderPan:R,crackleGains:I,electricGains:P,chordGain:X,chordFilter:W,chordPan:_,sub:z,sub2:L,subGain:V,wobbleFilter:O,wobbleLfo:H,wobbleFreqDepth:N,wobbleAmpDepth:Q,verbDayOut:i,verbNightOut:l}}(c=new AudioContext),document.addEventListener("pointerover",e=>{var t,n;m=!!(null==(n=e.target)||null==(t=n.closest)?void 0:t.call(n,"[data-audio-text]"))}),f=performance.now(),requestAnimationFrame(R)),c.resume(),u.master.gain.setTargetAtTime(.9,c.currentTime,.8)):c&&u&&(u.master.gain.setTargetAtTime(0,c.currentTime,.2),h=setTimeout(()=>{null==c||c.suspend()},900)),T.forEach(e=>e(v))}function F(){let e=null;try{e=localStorage.getItem("sound")}catch(e){}if("on"!==e||v)return()=>{};let t=()=>{n(),P(!0)},n=()=>{window.removeEventListener("pointerdown",t),window.removeEventListener("keydown",t)};return window.addEventListener("pointerdown",t,{once:!0}),window.addEventListener("keydown",t,{once:!0}),n}},970:(e,t,n)=>{n.d(t,{RT:()=>o,Ru:()=>r,lU:()=>i});let a={energy:0};function r(e){a.energy=Math.max(a.energy,Math.min(Math.max(e,0),1))}function i(){return a.energy}function o(e){a.energy*=e}},4576:(e,t,n)=>{n.d(t,{X:()=>i,b:()=>r});let a={stage:0,amt:0,top:2};function r(e){void 0!==e.stage&&(a.stage=Math.min(6,Math.max(0,e.stage))),void 0!==e.amt&&(a.amt=Math.min(1,Math.max(0,e.amt))),void 0!==e.top&&(a.top=Math.min(2,Math.max(-8,e.top)))}function i(){return a}},5001:(e,t,n)=>{n.d(t,{Qm:()=>i,RV:()=>o,bO:()=>r});let a=null;function r(e){a=e}function i(e){a===e&&(a=null)}function o(e){let t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:1;a?a.scrollTo(e,{duration:t,easing:e=>1-Math.pow(1-e,4)}):window.scrollTo({top:e,behavior:"smooth"})}},6234:(e,t,n)=>{n.d(t,{D5:()=>i,DJ:()=>o}),n(2115);let a=new Set,r=0;function i(e){r=e;let t=.01>Math.abs(e)?"":"skewY(".concat(e.toFixed(3),"deg)");for(let e of a)e.style.transform=t}function o(){return r}},6521:(e,t,n)=>{n.d(t,{Hl:()=>w,JX:()=>b,R:()=>m,UW:()=>d,fl:()=>f,lH:()=>r,nN:()=>g,t9:()=>x});var a=n(5339);let r=300,i=Array.from({length:300},()=>({x:0,y:0,scale:0,rot:0,opacity:0,visible:!1})),o={x:-1e4,y:1e4},l={x:0,y:0},s=0,c=0,u=-1,v=!1,h={x:-1e4,y:1e4,vx:0,vy:0,speed:0,angle:0};function d(){return h}function p(e){o.x=e.clientX-window.innerWidth/2,o.y=-(e.clientY-window.innerHeight/2)}function m(){v||(v=!0,window.addEventListener("pointermove",p,{passive:!0}))}function f(e){if(e===u)return;if(u=e,c+=.05,!(1>Math.abs(o.x-l.x)&&o.y-l.y<1)){let e=Math.min(Math.max(Math.floor(Math.hypot(o.x-l.x,o.y-l.y)/10),1),24);for(let t=1;t<=e;t++){let n=t/e,a=i[s=(s+1)%r];a.visible=!0,a.scale=.3,a.rot=0,a.x=l.x+(o.x-l.x)*n-.9,a.y=l.y+(o.y-l.y)*n,a.opacity=.22}}let t=Math.max(-80,Math.min(80,o.x-l.x)),n=Math.max(-80,Math.min(80,o.y-l.y));for(let e of(h.vx+=(t-h.vx)*.16,h.vy+=(n-h.vy)*.16,h.speed=Math.hypot(h.vx,h.vy),h.speed>.6&&(h.angle=Math.atan2(h.vy,h.vx)),h.x+=(o.x-h.x)*.4,h.y+=(o.y-h.y)*.4,l.x=o.x,l.y=o.y,i))e.visible&&(e.rot+=.02,e.opacity*=.955,e.opacity<.002&&(e.visible=!1),e.scale=.98*e.scale+.109)}function g(){return i}function w(){return c}let y=null;function x(){if(!y){let e=document.createElement("canvas");e.width=128,e.height=128;let t=e.getContext("2d"),n=t.createRadialGradient(64,64,0,64,64,64);n.addColorStop(0,"rgba(255,255,255,0.85)"),n.addColorStop(.3,"rgba(255,255,255,0.55)"),n.addColorStop(.65,"rgba(255,255,255,0.2)"),n.addColorStop(1,"rgba(255,255,255,0)"),t.fillStyle=n,t.fillRect(0,0,128,128),y=new a.GOR(e)}return y}let M=null;function b(){if(!M){let e=document.createElement("canvas");e.width=128,e.height=128;let t=e.getContext("2d"),n=t.createRadialGradient(64,64,0,64,64,64);n.addColorStop(0,"rgba(255,255,255,1)"),n.addColorStop(.35,"rgba(255,255,255,0.9)"),n.addColorStop(.7,"rgba(255,255,255,0.45)"),n.addColorStop(1,"rgba(255,255,255,0)"),t.fillStyle=n,t.fillRect(0,0,128,128),M=new a.GOR(e)}return M}},6820:(e,t,n)=>{n.d(t,{LP:()=>i,lI:()=>r,rf:()=>o});let a={canvas:null,version:0,amt:0};function r(e){a.canvas=e,a.version++}function i(e){a.amt=Math.min(1,Math.max(0,e))}function o(){return a}},7564:(e,t,n)=>{n.d(t,{os:()=>a.Ay,uY:()=>r.u});var a=n(7605),r=n(5580),i=n(3902);a.Ay.registerPlugin(r.u,i.s)},7923:(e,t,n)=>{n.d(t,{K:()=>u});var a=n(2115),r=n(7548),i=n(5339),o=n(6521),l=n(4576),s=n(6820),c=n(9245);function u(e){let{inverse:t=!1,fragment:n,mask:u}=arguments.length>1&&void 0!==arguments[1]?arguments[1]:{},[v,h]=(0,a.useState)(!0),d=(0,a.useRef)({inverse:t,fragment:n});return d.current={inverse:t,fragment:n},(0,a.useEffect)(()=>{var a;let v,p=e.current;if(!p)return;try{v=new r.JeP({canvas:p,antialias:!1,alpha:!0,powerPreference:"high-performance"})}catch(e){h(!1);return}v.setClearColor(0,0),(0,o.R)(),(0,c.GO)();let m=Math.min(window.devicePixelRatio||1,t||n?1.75:1.5);v.setPixelRatio(m);let f=new i.Z58,g=new i.qUd(-1,1,1,-1,-10,10),w=new i.nWS(1,1,{minFilter:i.k6q,magFilter:i.k6q,format:i.GWd}),y=new i.bdM(55,57),x=new i.uWO(new Float32Array(2*o.lH),2);x.setUsage(i.Vnu),y.setAttribute("aData",x);let M=new i.BKk({uniforms:{uMap:{value:(0,o.JX)()}},vertexShader:"\n        attribute vec2 aData; // x: opacity, y: freshness\n        varying vec2 vUv;\n        varying vec2 vData;\n        void main() {\n          vUv = uv;\n          vData = aData;\n          gl_Position = projectionMatrix * modelViewMatrix * instanceMatrix * vec4(position, 1.0);\n        }\n      ",fragmentShader:"\n        uniform sampler2D uMap;\n        varying vec2 vUv;\n        varying vec2 vData;\n        void main() {\n          float a = texture2D(uMap, vUv).a * vData.x;\n          gl_FragColor = vec4(1.0, vData.y, 1.0, a);\n        }\n      ",transparent:!0,blending:i.EZo,depthTest:!1,depthWrite:!1}),b=new i.ZLX(y,M,o.lH);b.instanceMatrix.setUsage(i.Vnu),b.frustumCulled=!1,f.add(b);let k=new i.B69,T=new i.BKk({uniforms:{uMap:{value:(0,o.t9)()},uAlpha:{value:.3},uFresh:{value:.55}},vertexShader:"\n        varying vec2 vUv;\n        void main() {\n          vUv = uv;\n          gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n        }\n      ",fragmentShader:"\n        uniform sampler2D uMap;\n        uniform float uAlpha;\n        uniform float uFresh;\n        varying vec2 vUv;\n        void main() {\n          float a = texture2D(uMap, vUv).a * uAlpha;\n          gl_FragColor = vec4(1.0, uFresh, 1.0, a);\n        }\n      ",transparent:!0,blending:i.EZo,depthTest:!1,depthWrite:!1}),A=new i.bdM(1,1),S=new i.eaF(A,T);S.frustumCulled=!1,f.add(S);let q={uTime:{value:0},uClock:{value:0},uSize:{value:new i.I9Y(1,1)},uOffset:{value:new i.I9Y(0,0)},uViewport:{value:new i.I9Y(1,1)},uSmoke:{value:w.texture},uMask:{value:null},uHasMask:{value:0},uThemePrev:{value:0},uThemeTarget:{value:0},uRippleT0:{value:-1e4},uRippleCenter:{value:new i.I9Y(.5,.5)},uStage:{value:0},uProcAmt:{value:0},uProcTop:{value:2},uInk:{value:null},uInkAmt:{value:0},uInverse:{value:+!!d.current.inverse}},C=new i.GYF(new Uint8Array([0,0,0,0]),1,1);C.needsUpdate=!0,q.uInk.value=C;let D=new i.Z58,R=new i.qUd(-1,1,1,-1,0,1),G=new i.eaF(new i.bdM(2,2),new i.BKk({vertexShader:"\n  varying vec2 vUv;\n  void main() {\n    vUv = uv;\n    gl_Position = vec4(position, 1.0);\n  }\n",fragmentShader:null!=(a=d.current.fragment)?a:"\n  precision highp float;\n\n  varying vec2 vUv;\n\n  uniform float uTime;     // their frame counter * 0.05\n  uniform float uClock;    // seconds, for the ripple only\n  uniform vec2 uSize;      // canvas size, CSS px\n  uniform vec2 uOffset;    // canvas offset from viewport top-left, CSS px\n  uniform vec2 uViewport;  // viewport size, CSS px\n  uniform sampler2D uSmoke; // smoke-trail render target\n  uniform sampler2D uMask;  // glyph mask (headline canvas only)\n  uniform float uHasMask;   // 0 = fullscreen background, 1 = masked text\n  uniform float uInverse;  // 0 = normal, 1 = inverted map (headline canvas)\n  uniform float uThemePrev;   // 0 = light, 1 = dark\n  uniform float uThemeTarget;\n  // Process-section material: holographic CMYK aura under analog grain\n  // (see the process block in main). As uStage walks 0 -> 4 the aura\n  // intensifies — colour saturation, swirl distortion and movement all\n  // ride the scroll, which is rate-capped upstream. 4 -> 6 is the release:\n  // the energy walks back down the SAME exponential curve, continuing\n  // across the NEXT section's approach, and only melts into the calm site\n  // field along organic noise borders (see meltMask) at the very tail.\n  // uProcAmt ramps the system in as the section approaches.\n  uniform float uStage;\n  uniform float uProcAmt;\n  // the process section's live top edge, viewport-normalized (y down; 0 =\n  // pinned fullscreen, 1 = below the fold). Unlike uStage/uProcAmt this is\n  // NOT time-smoothed — the aura is clipped to it so it can never bleed\n  // onto the sections above during a hard fling.\n  uniform float uProcTop;\n  // ink flood (services): a clicked word's glyphs drawn with a soft\n  // outward halo into a viewport-sized mask. As uInkAmt rises, the CMYK\n  // aura pours through the letter-shaped channels — glyph cores first,\n  // swelling through the halo — until the word floods the whole field;\n  // falling back to 0 disperses the ink into noise again.\n  uniform sampler2D uInk;\n  uniform float uInkAmt;\n  uniform float uRippleT0;    // seconds, same clock as uClock\n  uniform vec2 uRippleCenter; // viewport-normalized\n\n  // --- reference helpers, verbatim ----------------------------------------\n  float rand(vec2 st) {\n    return fract(sin(dot(st.xy, vec2(12.9898, 78.233))) * 43758.5453123);\n  }\n\n  float ns(vec2 p) {\n    vec2 ip = floor(p);\n    vec2 u = fract(p);\n    u = u * u * (3.0 - 2.0 * u);\n    float res = mix(\n      mix(rand(ip), rand(ip + vec2(1.0, 0.0)), u.x),\n      mix(rand(ip + vec2(0.0, 1.0)), rand(ip + vec2(1.0, 1.0)), u.x), u.y);\n    return res * res;\n  }\n\n  // ---- process-material helpers -------------------------------------------\n\n  // noise-threshold dissolve: as p goes 0 -> 1, the next material eats into\n  // the previous one along slowly-drifting organic borders\n  float meltMask(vec2 uvUp, float time, float p, float seed) {\n    if (p <= 0.0) return 0.0;\n    if (p >= 1.0) return 1.0;\n    // broad blobs and a wide threshold band: the next material seeps in as\n    // large soft pools rather than crackling in along tight noise edges\n    float nz = ns(uvUp * 1.6 + vec2(time * 0.012, time * -0.009) + vec2(seed, seed * 1.7));\n    float t = p * 1.7 - 0.35;\n    return smoothstep(nz - 0.35, nz + 0.35, t);\n  }\n\n  void main() {\n    // vUv.y grows upward but CSS/viewport coords grow downward — flip it\n    vec2 screen = uOffset + vec2(vUv.x, 1.0 - vUv.y) * uSize;\n    vec2 uvV = screen / uViewport;               // 0..1, y down (for the ripple)\n    vec2 uvUp = vec2(uvV.x, 1.0 - uvV.y);        // their plane-uv space, y up\n\n    float time = uTime;\n\n    // --- theme ripple (ours) ------------------------------------------------\n    float aspect = uViewport.x / max(uViewport.y, 1.0);\n    vec2 pA = uvV; pA.x *= aspect;\n    vec2 rc = uRippleCenter; rc.x *= aspect;\n    float rdist = distance(pA, rc);\n    float age = uClock - uRippleT0;\n    float rr = age * 0.9;\n    float rw = 0.16 + rr * 0.1;\n    float adoption = clamp(1.0 - smoothstep(rr - rw, rr, rdist), 0.0, 1.0);\n    float th = mix(uThemePrev, uThemeTarget, adoption);\n    float frontGlow = exp(-pow((rdist - rr) / max(rw, 1e-3), 2.0) * 2.5)\n                    * exp(-age * 0.55) * step(0.0, age);\n\n    // --- background + cursor, reference math verbatim ------------------------\n    vec4 n = texture2D(uSmoke, uvUp);\n    float z = n.r * 2. * 3.14159265359;\n    vec2 dr = vec2(sin(z));\n    // n.g carries freshness — the front of the trail distorts (and melts the\n    // text) much more than the dissipating back\n    vec2 dvec = dr * (n.r * 0.035 + n.g * 0.11);\n    vec2 uv = uvUp + dvec;\n\n    float d1 = distance(uv, vec2(0.5, 0.5));\n    float d2 = distance(uv, vec2(0.0, 1.0));\n    // stronger grain in light mode, where the low field contrast used to\n    // swallow it\n    float grAmp = mix(0.36, 0.2, th);\n    float gr = mix(-grAmp, grAmp, rand(uv + sin(time)));\n    vec2 mv = vec2(time * 0.05, time * -0.05);\n    float f = ns((uv * d1 * 2.) + mv);\n    f += ns((uv * d2 * 2.5) + vec2(time * -0.075, time * 0.05));\n    f += gr;\n    f = smoothstep(0., 2., f);\n\n    // base field palettes: pastel-tinted instead of pure greys. A slow hue\n    // drift blends lilac/blush/mint in light, and their really dark\n    // counterparts (violet/blue/green, near the original 0.3 luminance) in\n    // dark — swapped by the ripple like before.\n    float hue = ns(uv * 1.2 + vec2(time * 0.03, time * -0.02));\n    float hue2 = ns(uv * 0.9 - vec2(time * 0.025, time * 0.03));\n\n    vec3 c1L = vec3(0.985, 0.975, 1.0);\n    vec3 c2L = vec3(0.660, 0.615, 0.840);                                       // lilac\n    c2L = mix(c2L, vec3(0.845, 0.615, 0.690), smoothstep(0.3, 0.8, hue));       // blush\n    c2L = mix(c2L, vec3(0.585, 0.760, 0.680), smoothstep(0.35, 0.85, hue2) * 0.8); // mint\n\n    vec3 c1D = vec3(0.020, 0.015, 0.040);\n    vec3 c2D = vec3(0.235, 0.205, 0.330);                                       // dark violet\n    c2D = mix(c2D, vec3(0.165, 0.205, 0.305), smoothstep(0.3, 0.8, hue));       // dark blue\n    c2D = mix(c2D, vec3(0.175, 0.245, 0.215), smoothstep(0.35, 0.85, hue2) * 0.7); // dark green\n\n    vec3 c1 = mix(c1L, c1D, th);\n    vec3 c2 = mix(c2L, c2D, th);\n    vec3 col = mix(c1, c2, f);\n\n    col += frontGlow * 0.10;\n\n    if (uInverse > 0.5) {\n      col = vec3(1.0) - col;\n    }\n\n    // --- process-material gate, computed early so the trail below can\n    // cross-fade against it. uStage 0..4 builds the aura exponentially,\n    // 4..6 releases back down the same curve in one unbroken slope.\n    float s = uStage;\n    float sb = max(min(s, 4.0) - max(0.0, s - 4.0) * 2.0, 0.0);\n    float I = clamp((exp2(sb * 0.5) - 1.0) / 3.0, 0.0, 1.0);\n    // the organic hand-back starts deep into the release and only finishes\n    // once the next section fills the viewport\n    float fadeBack = clamp((s - 4.8) / 1.2, 0.0, 1.0);\n    // spatial clip: the aura blooms in from the section's side of the\n    // seam as it scrolls in, and wipes out with it on the way back up.\n    // A CONSTANT-width ramp (never a tightening band — that read as a\n    // hard cutout) dissolves into full coverage across the last half\n    // viewport of the approach, so by the time the section pins the\n    // gradient has already melted away instead of compressing into a\n    // line. The brief soft tint that crosses the seam mid-transition is\n    // the same gradual wash the release shows on the far side. uProcTop\n    // tracks the layout directly, so this holds at ANY scroll speed.\n    float doneness = 1.0 - clamp(uProcTop * 2.0, 0.0, 1.0);\n    float clipTop = mix(smoothstep(uProcTop, uProcTop + 0.6, uvV.y), 1.0, doneness);\n    float vis = uProcAmt * clipTop * (1.0 - meltMask(uvUp, time, fadeBack, 53.9));\n\n    // --- ink flood: the clicked word's soft-halo mask, liquefied by a\n    // drifting warp and cut by a threshold sweep that starts at the glyph\n    // cores and finishes past the softest halo — the word pours outward\n    // until it IS the field, then disperses the same way in reverse\n    float flood = 0.0;\n    if (uInkAmt > 0.003) {\n      vec2 iw = uvUp + dvec * 0.8;\n      iw += (vec2(ns(iw * 3.1 + vec2(time * 0.07, 0.0)),\n                  ns(iw * 3.1 - vec2(0.0, time * 0.05))) - 0.5)\n            * (0.02 + 0.10 * uInkAmt);\n      float inkA = texture2D(uInk, iw).a;\n      float nz2 = ns((uvUp + dvec) * vec2(aspect, 1.0) * 2.6\n                     + vec2(time * 0.06, -time * 0.04));\n      float edge0 = 0.82 - uInkAmt * 1.25;\n      flood = smoothstep(edge0, edge0 + 0.22, inkA + (nz2 - 0.5) * 0.28);\n    }\n\n    // --- color gradient riding the smoke trail -------------------------------\n    // Theme-split palettes, blended by drifting noise and revealed by the\n    // trail's intensity. Applied after inversion so it reads the same inside\n    // the headline glyphs. The trail inherits the field's grain (via f, which\n    // contains gr) and never fully replaces the background, so it feels like\n    // part of it rather than a layer on top.\n    float p1 = ns(uv * 3.0 + vec2(time * 0.1, 0.));\n    float p2 = ns(uv * 4.0 - vec2(0., time * 0.12));\n    float p3 = ns(uv * 2.5 + vec2(time * 0.07, -time * 0.05));\n\n    // light: purple base -> pink / yellow / green\n    vec3 vividL = vec3(0.545, 0.361, 0.965);\n    vividL = mix(vividL, vec3(0.925, 0.282, 0.600), smoothstep(0.25, 0.85, p1));\n    vividL = mix(vividL, vec3(0.980, 0.800, 0.082), smoothstep(0.35, 0.90, p2) * 0.8);\n    vividL = mix(vividL, vec3(0.133, 0.773, 0.420), smoothstep(0.40, 0.92, p3) * 0.7);\n\n    // dark: purple base -> dark blue / violet, faint yellow + green accents,\n    // pink core near the cursor head\n    vec3 vividD = vec3(0.486, 0.227, 0.929);\n    vividD = mix(vividD, vec3(0.118, 0.227, 0.541), smoothstep(0.30, 0.90, p2) * 0.85);\n    vividD = mix(vividD, vec3(0.545, 0.361, 0.965), smoothstep(0.35, 0.90, p3) * 0.6);\n    vividD = mix(vividD, vec3(0.980, 0.800, 0.082), smoothstep(0.55, 0.95, p1 * p3) * 0.25);\n    vividD = mix(vividD, vec3(0.133, 0.773, 0.420), smoothstep(0.50, 0.95, p2 * p3) * 0.25);\n    vividD = mix(vividD, vec3(0.925, 0.282, 0.600), smoothstep(0.45, 0.95, n.r) * 0.75);\n\n    vec3 vivid = mix(vividL, vividD, th);\n    vivid *= 0.8 + 0.45 * f;                       // field texture + grain in the trail\n    float colorAmt = smoothstep(0.04, 0.9, n.r + gr * 0.5);\n    colorAmt = min(colorAmt, 0.8);                 // background always shows through\n    // the aura mix below would cover this base-pass trail, so it fades\n    // out here and a lighter, grainier version of the SAME trail is\n    // re-laid on top of the aura inside the process block\n    float trailBase = colorAmt;\n    colorAmt *= 1.0 - max(vis, flood);\n    col = mix(col, vivid, colorAmt);\n\n    // --- the process material: holographic CMYK aura ------------------------\n    // One continuous field, generalized from the analog-grain holographic\n    // aura refs. Two full-spectrum sheets ride a domain-warped swirl; where\n    // the sheets disagree the mix greys out into the muted zones between\n    // ribbons, exactly like the references. Everything is graded by\n    // progress: uStage 0..4 walks the aura from a faint pastel wash —\n    // barely more than the site field, same grain — into full CMYK\n    // saturation with deep swirl distortion and thicker film grain, and\n    // 4..5 melts it back into the calm background along organic noise\n    // borders. Movement is SCROLL-DRIVEN: the flow phase and the swirl\n    // rotation ride uStage (rate-capped upstream), so every scroll trigger\n    // stirs and distorts the field, and at rest it only barely breathes.\n    // The cursor trail keeps displacing it throughout.\n    if (vis > 0.003 || flood > 0.003) {\n      // effective intensity: the scroll-driven curve, or — when a word is\n      // pouring — the flood forces the aura near its full-energy state so\n      // the ink arrives alive rather than as the faint first-pane wash\n      float Ie = max(I, uInkAmt * 0.9);\n      // the flow phase rides the SAME exponential curve, so scrolling stirs\n      // harder pane by pane; the ambient floor is deliberately high so the\n      // field is always visibly flowing, even parked on the first pane\n      float ph = Ie * 4.4 + time * (0.42 + 0.18 * Ie);\n      float zoom = 1.45 + 0.15 * Ie;\n      vec2 sp = (uvUp + dvec * (1.5 + 1.0 * Ie)) * vec2(aspect, 1.0) * zoom;\n      // swirl rotation about the viewport centre: scroll-driven, with a\n      // steady constant churn while static\n      float ang = Ie * 0.88 + time * 0.028;\n      vec2 ctr = vec2(aspect, 1.0) * zoom * 0.5;\n      vec2 rp = sp - ctr;\n      sp = ctr + mat2(cos(ang), -sin(ang), sin(ang), cos(ang)) * rp;\n\n      // domain warp deepens with progress, but even the first pane gets\n      // real curl and fine detail — the wash swirls, it doesn't sit flat\n      vec2 q = vec2(\n        ns(sp * 0.8 + vec2(ph * 0.14, 3.0)),\n        ns(sp * 0.8 - vec2(7.0, ph * 0.11)));\n      vec2 q2 = vec2(\n        ns(sp * 1.7 + q * 2.0 + vec2(11.0, ph * 0.07)),\n        ns(sp * 1.7 + q * 2.0 - vec2(ph * 0.06, 4.0)));\n      vec2 fp = sp + (q - 0.5) * (1.5 + 1.9 * Ie) + (q2 - 0.5) * (0.8 + 1.0 * Ie);\n\n      // two full-spectrum sheets (ns is biased dark — sqrt flattens); the\n      // wheel rotates a touch with the scroll so each pane lands on its\n      // own slice of the spectrum\n      float t1 = sqrt(ns(fp * 0.85));\n      float t2 = sqrt(ns(fp * 1.45 + 19.0));\n      vec3 w1 = 0.5 + 0.5 * cos(6.28318 * (0.02 + s * 0.05 + t1 * 0.9 + vec3(0.0, 0.33, 0.66)));\n      vec3 w2 = 0.5 + 0.5 * cos(6.28318 * (0.55 + s * 0.05 + t2 * 0.9 + vec3(0.0, 0.33, 0.66)));\n      vec3 aura = w1 * 0.62 + w2 * 0.38;\n      aura = mix(aura, vec3(1.0), 0.22);      // holographic milk\n\n      // progress grading: soft-but-present pastel wash -> full CMYK\n      float lum = dot(aura, vec3(0.299, 0.587, 0.114));\n      aura = mix(vec3(lum), aura, 0.45 + 0.55 * Ie);\n      aura = mix(aura, aura * vec3(0.40, 0.38, 0.52) + vec3(0.03), th);\n\n      // analog film grain, per-channel so it sparkles slightly: reads like\n      // the site field at the start and thickens as the aura takes over\n      float g2 = mix(-grAmp, grAmp, rand(uv * 1.31 + 4.7 + sin(time)));\n      aura += vec3(gr, mix(gr, g2, 0.5), g2) * (0.20 + 0.14 * Ie);\n\n      if (uInverse > 0.5) aura = vec3(1.0) - aura;\n      col = mix(col, aura, max(vis * (0.42 + 0.58 * Ie), flood));\n\n      // --- the standard site cursor, re-laid over the aura: the same\n      // vivid trail as everywhere else, just lighter and with extra film\n      // grain worked into it so it melts into the analog field. Applied\n      // after the inversion, like the base pass, so it reads the same\n      // inside the headline glyphs.\n      col = mix(col, vivid + gr * 0.55, trailBase * 0.55 * max(vis, flood));\n    }\n\n    // glyph mask sampled at the SAME displaced uv, so the smoke trail melts\n    // the letterforms exactly like the reference warps its media planes\n    float alpha = 1.0;\n    if (uHasMask > 0.5) {\n      vec2 mUv = vUv + dvec * (uViewport / uSize);\n      alpha = texture2D(uMask, mUv).a;\n    }\n\n    gl_FragColor = vec4(col, alpha);\n  }\n",uniforms:q,transparent:!0}));G.frustumCulled=!1,D.add(G);let I=()=>{let e=p.getBoundingClientRect(),t=Math.max(e.width,1),n=Math.max(e.height,1);v.setSize(t,n,!1),q.uSize.value.set(t,n);let a=window.innerWidth,r=window.innerHeight;q.uViewport.value.set(a,r),w.setSize(Math.ceil(a/3),Math.ceil(r/3)),g.left=-a/2,g.right=a/2,g.top=r/2,g.bottom=-r/2,g.updateProjectionMatrix()};I();let U=new ResizeObserver(I);U.observe(p),window.addEventListener("resize",I);let P=!0,F=new IntersectionObserver(e=>{var t,n;P=null==(n=null==(t=e[0])?void 0:t.isIntersecting)||n});F.observe(p);let B=0,z=-1,L=null,E=-1,O=null,Y=!d.current.inverse&&!d.current.fragment&&!u,V=0,H="",N=new Uint8Array(4),Q=e=>{if(B=requestAnimationFrame(Q),(0,o.fl)(e),!P)return;let t=null==u?void 0:u.current;t&&t.version!==z&&(z=t.version,null==L||L.dispose(),L=new i.GOR(t.canvas),q.uMask.value=L,q.uHasMask.value=1);let n=(0,o.nN)();for(let e=0;e<o.lH;e++){let t=n[e];t.visible?(k.position.set(t.x,t.y,0),k.rotation.z=t.rot,k.scale.set(t.scale,t.scale,1)):(k.position.set(0,0,0),k.rotation.z=0,k.scale.set(0,0,0)),k.updateMatrix(),b.setMatrixAt(e,k.matrix);let a=Math.min(t.opacity/.22,1);x.setXY(e,t.visible?t.opacity:0,a*a)}b.instanceMatrix.needsUpdate=!0,x.needsUpdate=!0;let a=(0,o.UW)(),r=1+Math.min(.04*a.speed,1.7),h=190*(r-1)*.4;S.position.set(a.x-Math.cos(a.angle)*h,a.y-Math.sin(a.angle)*h,0),S.rotation.z=a.angle,S.scale.set(190*r,190/(1+(r-1)*.4),1);let y=(0,c.rP)(),M=p.getBoundingClientRect();q.uOffset.value.set(M.left,M.top),q.uTime.value=(0,o.Hl)(),q.uClock.value=e/1e3,q.uThemePrev.value=y.prev,q.uThemeTarget.value=y.target,q.uRippleT0.value=y.rippleT0,q.uRippleCenter.value.set(y.center.x,y.center.y);let T=(0,l.X)();q.uStage.value=T.stage,q.uProcAmt.value=T.amt,q.uProcTop.value=T.top;let A=(0,s.rf)();if(A.version!==E&&(E=A.version,null==O||O.dispose(),O=A.canvas?new i.GOR(A.canvas):null,q.uInk.value=null!=O?O:C),q.uInkAmt.value=A.amt,q.uInverse.value=+!!d.current.inverse,v.setRenderTarget(w),v.render(f,g),v.setRenderTarget(null),v.render(D,R),Y&&(V-=1)<=0){V=24;let e=v.getContext(),t=Math.floor(e.drawingBufferWidth/2),n=Math.max(0,e.drawingBufferHeight-Math.ceil(6*m));if(e.readPixels(t,n,1,1,e.RGBA,e.UNSIGNED_BYTE,N),N[3]>0){let e=N[0],t=N[1],n=N[2],a=(.2126*e+.7152*t+.0722*n)/255;if(a>.8){let r=.8/a;e=Math.round(e*r),t=Math.round(t*r),n=Math.round(n*r)}let r="#".concat((0x1000000|e<<16|t<<8|n).toString(16).slice(1));if(r!==H){H=r;let e=document.querySelector('meta[name="theme-color"]');e&&(e.content=r),document.body.style.backgroundColor=r}}}};return B=requestAnimationFrame(Q),()=>{cancelAnimationFrame(B),U.disconnect(),F.disconnect(),window.removeEventListener("resize",I),null==L||L.dispose(),null==O||O.dispose(),C.dispose(),M.dispose(),y.dispose(),T.dispose(),A.dispose(),w.dispose(),G.geometry.dispose(),G.material.dispose(),v.dispose()}},[e]),v}},9245:(e,t,n)=>{n.d(t,{GO:()=>i,Yl:()=>o,rP:()=>l});let a={prev:0,target:0,rippleT0:-1e4,center:{x:.5,y:.5}},r=!1;function i(){if(r||"undefined"==typeof document)return;r=!0;let e=+("dark"===document.documentElement.dataset.theme);a.prev=e,a.target=e}function o(e){let t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:.5,n=arguments.length>2&&void 0!==arguments[2]?arguments[2]:.5,r=!(arguments.length>3)||void 0===arguments[3]||arguments[3];i();let o=+("dark"===e);if(o===a.target)return;a.prev=a.target,a.target=o,a.rippleT0=performance.now()/1e3,a.center={x:t,y:n},document.documentElement.dataset.theme=e;let l=document.querySelector('meta[name="theme-color"]');if(l&&(l.content="dark"===e?"#0C0B10":"#CBC5D5"),r)try{localStorage.setItem("theme",e)}catch(e){}}function l(){return a}}}]);