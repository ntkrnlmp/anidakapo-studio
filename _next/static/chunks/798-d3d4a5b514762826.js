"use strict";(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[798],{4576:(e,t,n)=>{n.d(t,{X:()=>r,b:()=>i});let a={stage:0,amt:0,top:2};function i(e){void 0!==e.stage&&(a.stage=Math.min(6,Math.max(0,e.stage))),void 0!==e.amt&&(a.amt=Math.min(1,Math.max(0,e.amt))),void 0!==e.top&&(a.top=Math.min(2,Math.max(-8,e.top)))}function r(){return a}},5001:(e,t,n)=>{n.d(t,{Qm:()=>r,RV:()=>o,bO:()=>i});let a=null;function i(e){a=e}function r(e){a===e&&(a=null)}function o(e){let t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:1;a?a.scrollTo(e,{duration:t,easing:e=>1-Math.pow(1-e,4)}):window.scrollTo({top:e,behavior:"smooth"})}},6521:(e,t,n)=>{n.d(t,{Hl:()=>w,JX:()=>b,R:()=>m,UW:()=>h,fl:()=>f,lH:()=>i,nN:()=>g,t9:()=>y});var a=n(5339);let i=300,r=Array.from({length:300},()=>({x:0,y:0,scale:0,rot:0,opacity:0,visible:!1})),o={x:-1e4,y:1e4},s={x:0,y:0},l=0,c=0,u=-1,v=!1,d={x:-1e4,y:1e4,vx:0,vy:0,speed:0,angle:0};function h(){return d}function p(e){o.x=e.clientX-window.innerWidth/2,o.y=-(e.clientY-window.innerHeight/2)}function m(){v||(v=!0,window.addEventListener("pointermove",p,{passive:!0}))}function f(e){if(e===u)return;if(u=e,c+=.05,!(1>Math.abs(o.x-s.x)&&o.y-s.y<1)){let e=Math.min(Math.max(Math.floor(Math.hypot(o.x-s.x,o.y-s.y)/10),1),24);for(let t=1;t<=e;t++){let n=t/e,a=r[l=(l+1)%i];a.visible=!0,a.scale=.3,a.rot=0,a.x=s.x+(o.x-s.x)*n-.9,a.y=s.y+(o.y-s.y)*n,a.opacity=.22}}let t=Math.max(-80,Math.min(80,o.x-s.x)),n=Math.max(-80,Math.min(80,o.y-s.y));for(let e of(d.vx+=(t-d.vx)*.16,d.vy+=(n-d.vy)*.16,d.speed=Math.hypot(d.vx,d.vy),d.speed>.6&&(d.angle=Math.atan2(d.vy,d.vx)),d.x+=(o.x-d.x)*.4,d.y+=(o.y-d.y)*.4,s.x=o.x,s.y=o.y,r))e.visible&&(e.rot+=.02,e.opacity*=.955,e.opacity<.002&&(e.visible=!1),e.scale=.98*e.scale+.109)}function g(){return r}function w(){return c}let x=null;function y(){if(!x){let e=document.createElement("canvas");e.width=128,e.height=128;let t=e.getContext("2d"),n=t.createRadialGradient(64,64,0,64,64,64);n.addColorStop(0,"rgba(255,255,255,0.85)"),n.addColorStop(.3,"rgba(255,255,255,0.55)"),n.addColorStop(.65,"rgba(255,255,255,0.2)"),n.addColorStop(1,"rgba(255,255,255,0)"),t.fillStyle=n,t.fillRect(0,0,128,128),x=new a.GOR(e)}return x}let k=null;function b(){if(!k){let e=document.createElement("canvas");e.width=128,e.height=128;let t=e.getContext("2d"),n=t.createRadialGradient(64,64,0,64,64,64);n.addColorStop(0,"rgba(255,255,255,1)"),n.addColorStop(.35,"rgba(255,255,255,0.9)"),n.addColorStop(.7,"rgba(255,255,255,0.45)"),n.addColorStop(1,"rgba(255,255,255,0)"),t.fillStyle=n,t.fillRect(0,0,128,128),k=new a.GOR(e)}return k}},6820:(e,t,n)=>{n.d(t,{LP:()=>r,lI:()=>i,rf:()=>o});let a={canvas:null,version:0,amt:0};function i(e){a.canvas=e,a.version++}function r(e){a.amt=Math.min(1,Math.max(0,e))}function o(){return a}},7564:(e,t,n)=>{n.d(t,{os:()=>a.Ay,uY:()=>i.u});var a=n(7605),i=n(5580),r=n(3902);a.Ay.registerPlugin(i.u,r.s)},7923:(e,t,n)=>{n.d(t,{K:()=>u});var a=n(2115),i=n(7548),r=n(5339),o=n(6521),s=n(4576),l=n(6820),c=n(9245);function u(e){let{inverse:t=!1,fragment:n,mask:u}=arguments.length>1&&void 0!==arguments[1]?arguments[1]:{},[v,d]=(0,a.useState)(!0),h=(0,a.useRef)({inverse:t,fragment:n});return h.current={inverse:t,fragment:n},(0,a.useEffect)(()=>{var a;let v,p=e.current;if(!p)return;try{v=new i.JeP({canvas:p,antialias:!1,alpha:!0,powerPreference:"high-performance"})}catch(e){d(!1);return}v.setClearColor(0,0),(0,o.R)(),(0,c.GO)();let m=Math.min(window.devicePixelRatio||1,t||n?1.75:1.5);v.setPixelRatio(m);let f=new r.Z58,g=new r.qUd(-1,1,1,-1,-10,10),w=new r.nWS(1,1,{minFilter:r.k6q,magFilter:r.k6q,format:r.GWd}),x=new r.bdM(55,57),y=new r.uWO(new Float32Array(2*o.lH),2);y.setUsage(r.Vnu),x.setAttribute("aData",y);let k=new r.BKk({uniforms:{uMap:{value:(0,o.JX)()}},vertexShader:"\n        attribute vec2 aData; // x: opacity, y: freshness\n        varying vec2 vUv;\n        varying vec2 vData;\n        void main() {\n          vUv = uv;\n          vData = aData;\n          gl_Position = projectionMatrix * modelViewMatrix * instanceMatrix * vec4(position, 1.0);\n        }\n      ",fragmentShader:"\n        uniform sampler2D uMap;\n        varying vec2 vUv;\n        varying vec2 vData;\n        void main() {\n          float a = texture2D(uMap, vUv).a * vData.x;\n          gl_FragColor = vec4(1.0, vData.y, 1.0, a);\n        }\n      ",transparent:!0,blending:r.EZo,depthTest:!1,depthWrite:!1}),b=new r.ZLX(x,k,o.lH);b.instanceMatrix.setUsage(r.Vnu),b.frustumCulled=!1,f.add(b);let M=new r.B69,A=new r.BKk({uniforms:{uMap:{value:(0,o.t9)()},uAlpha:{value:.3},uFresh:{value:.55}},vertexShader:"\n        varying vec2 vUv;\n        void main() {\n          vUv = uv;\n          gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n        }\n      ",fragmentShader:"\n        uniform sampler2D uMap;\n        uniform float uAlpha;\n        uniform float uFresh;\n        varying vec2 vUv;\n        void main() {\n          float a = texture2D(uMap, vUv).a * uAlpha;\n          gl_FragColor = vec4(1.0, uFresh, 1.0, a);\n        }\n      ",transparent:!0,blending:r.EZo,depthTest:!1,depthWrite:!1}),S=new r.bdM(1,1),T=new r.eaF(S,A);T.frustumCulled=!1,f.add(T);let C={uTime:{value:0},uClock:{value:0},uSize:{value:new r.I9Y(1,1)},uOffset:{value:new r.I9Y(0,0)},uViewport:{value:new r.I9Y(1,1)},uSmoke:{value:w.texture},uMask:{value:null},uHasMask:{value:0},uThemePrev:{value:0},uThemeTarget:{value:0},uRippleT0:{value:-1e4},uRippleCenter:{value:new r.I9Y(.5,.5)},uStage:{value:0},uProcAmt:{value:0},uProcTop:{value:2},uInk:{value:null},uInkAmt:{value:0},uInverse:{value:+!!h.current.inverse}},I=new r.GYF(new Uint8Array([0,0,0,0]),1,1);I.needsUpdate=!0,C.uInk.value=I;let D=new r.Z58,U=new r.qUd(-1,1,1,-1,0,1),P=new r.eaF(new r.bdM(2,2),new r.BKk({vertexShader:"\n  varying vec2 vUv;\n  void main() {\n    vUv = uv;\n    gl_Position = vec4(position, 1.0);\n  }\n",fragmentShader:null!=(a=h.current.fragment)?a:"\n  precision highp float;\n\n  varying vec2 vUv;\n\n  uniform float uTime;     // their frame counter * 0.05\n  uniform float uClock;    // seconds, for the ripple only\n  uniform vec2 uSize;      // canvas size, CSS px\n  uniform vec2 uOffset;    // canvas offset from viewport top-left, CSS px\n  uniform vec2 uViewport;  // viewport size, CSS px\n  uniform sampler2D uSmoke; // smoke-trail render target\n  uniform sampler2D uMask;  // glyph mask (headline canvas only)\n  uniform float uHasMask;   // 0 = fullscreen background, 1 = masked text\n  uniform float uInverse;  // 0 = normal, 1 = inverted map (headline canvas)\n  uniform float uThemePrev;   // 0 = light, 1 = dark\n  uniform float uThemeTarget;\n  // Process-section material: holographic CMYK aura under analog grain\n  // (see the process block in main). As uStage walks 0 -> 4 the aura\n  // intensifies — colour saturation, swirl distortion and movement all\n  // ride the scroll, which is rate-capped upstream. 4 -> 6 is the release:\n  // the energy walks back down the SAME exponential curve, continuing\n  // across the NEXT section's approach, and only melts into the calm site\n  // field along organic noise borders (see meltMask) at the very tail.\n  // uProcAmt ramps the system in as the section approaches.\n  uniform float uStage;\n  uniform float uProcAmt;\n  // the process section's live top edge, viewport-normalized (y down; 0 =\n  // pinned fullscreen, 1 = below the fold). Unlike uStage/uProcAmt this is\n  // NOT time-smoothed — the aura is clipped to it so it can never bleed\n  // onto the sections above during a hard fling.\n  uniform float uProcTop;\n  // ink flood (services): a clicked word's glyphs drawn with a soft\n  // outward halo into a viewport-sized mask. As uInkAmt rises, the CMYK\n  // aura pours through the letter-shaped channels — glyph cores first,\n  // swelling through the halo — until the word floods the whole field;\n  // falling back to 0 disperses the ink into noise again.\n  uniform sampler2D uInk;\n  uniform float uInkAmt;\n  uniform float uRippleT0;    // seconds, same clock as uClock\n  uniform vec2 uRippleCenter; // viewport-normalized\n\n  // --- reference helpers, verbatim ----------------------------------------\n  float rand(vec2 st) {\n    return fract(sin(dot(st.xy, vec2(12.9898, 78.233))) * 43758.5453123);\n  }\n\n  float ns(vec2 p) {\n    vec2 ip = floor(p);\n    vec2 u = fract(p);\n    u = u * u * (3.0 - 2.0 * u);\n    float res = mix(\n      mix(rand(ip), rand(ip + vec2(1.0, 0.0)), u.x),\n      mix(rand(ip + vec2(0.0, 1.0)), rand(ip + vec2(1.0, 1.0)), u.x), u.y);\n    return res * res;\n  }\n\n  // ---- process-material helpers -------------------------------------------\n\n  // noise-threshold dissolve: as p goes 0 -> 1, the next material eats into\n  // the previous one along slowly-drifting organic borders\n  float meltMask(vec2 uvUp, float time, float p, float seed) {\n    if (p <= 0.0) return 0.0;\n    if (p >= 1.0) return 1.0;\n    // broad blobs and a wide threshold band: the next material seeps in as\n    // large soft pools rather than crackling in along tight noise edges\n    float nz = ns(uvUp * 1.6 + vec2(time * 0.012, time * -0.009) + vec2(seed, seed * 1.7));\n    float t = p * 1.7 - 0.35;\n    return smoothstep(nz - 0.35, nz + 0.35, t);\n  }\n\n  void main() {\n    // vUv.y grows upward but CSS/viewport coords grow downward — flip it\n    vec2 screen = uOffset + vec2(vUv.x, 1.0 - vUv.y) * uSize;\n    vec2 uvV = screen / uViewport;               // 0..1, y down (for the ripple)\n    vec2 uvUp = vec2(uvV.x, 1.0 - uvV.y);        // their plane-uv space, y up\n\n    float time = uTime;\n\n    // --- theme ripple (ours) ------------------------------------------------\n    float aspect = uViewport.x / max(uViewport.y, 1.0);\n    vec2 pA = uvV; pA.x *= aspect;\n    vec2 rc = uRippleCenter; rc.x *= aspect;\n    float rdist = distance(pA, rc);\n    float age = uClock - uRippleT0;\n    float rr = age * 0.9;\n    float rw = 0.16 + rr * 0.1;\n    float adoption = clamp(1.0 - smoothstep(rr - rw, rr, rdist), 0.0, 1.0);\n    float th = mix(uThemePrev, uThemeTarget, adoption);\n    float frontGlow = exp(-pow((rdist - rr) / max(rw, 1e-3), 2.0) * 2.5)\n                    * exp(-age * 0.55) * step(0.0, age);\n\n    // --- background + cursor, reference math verbatim ------------------------\n    vec4 n = texture2D(uSmoke, uvUp);\n    float z = n.r * 2. * 3.14159265359;\n    vec2 dr = vec2(sin(z));\n    // n.g carries freshness — the front of the trail distorts (and melts the\n    // text) much more than the dissipating back\n    vec2 dvec = dr * (n.r * 0.035 + n.g * 0.11);\n    vec2 uv = uvUp + dvec;\n\n    float d1 = distance(uv, vec2(0.5, 0.5));\n    float d2 = distance(uv, vec2(0.0, 1.0));\n    // stronger grain in light mode, where the low field contrast used to\n    // swallow it\n    float grAmp = mix(0.36, 0.2, th);\n    float gr = mix(-grAmp, grAmp, rand(uv + sin(time)));\n    vec2 mv = vec2(time * 0.05, time * -0.05);\n    float f = ns((uv * d1 * 2.) + mv);\n    f += ns((uv * d2 * 2.5) + vec2(time * -0.075, time * 0.05));\n    f += gr;\n    f = smoothstep(0., 2., f);\n\n    // base field palettes: pastel-tinted instead of pure greys. A slow hue\n    // drift blends lilac/blush/mint in light, and their really dark\n    // counterparts (violet/blue/green, near the original 0.3 luminance) in\n    // dark — swapped by the ripple like before.\n    float hue = ns(uv * 1.2 + vec2(time * 0.03, time * -0.02));\n    float hue2 = ns(uv * 0.9 - vec2(time * 0.025, time * 0.03));\n\n    vec3 c1L = vec3(0.985, 0.975, 1.0);\n    vec3 c2L = vec3(0.660, 0.615, 0.840);                                       // lilac\n    c2L = mix(c2L, vec3(0.845, 0.615, 0.690), smoothstep(0.3, 0.8, hue));       // blush\n    c2L = mix(c2L, vec3(0.585, 0.760, 0.680), smoothstep(0.35, 0.85, hue2) * 0.8); // mint\n\n    vec3 c1D = vec3(0.020, 0.015, 0.040);\n    vec3 c2D = vec3(0.235, 0.205, 0.330);                                       // dark violet\n    c2D = mix(c2D, vec3(0.165, 0.205, 0.305), smoothstep(0.3, 0.8, hue));       // dark blue\n    c2D = mix(c2D, vec3(0.175, 0.245, 0.215), smoothstep(0.35, 0.85, hue2) * 0.7); // dark green\n\n    vec3 c1 = mix(c1L, c1D, th);\n    vec3 c2 = mix(c2L, c2D, th);\n    vec3 col = mix(c1, c2, f);\n\n    col += frontGlow * 0.10;\n\n    if (uInverse > 0.5) {\n      col = vec3(1.0) - col;\n    }\n\n    // --- process-material gate, computed early so the trail below can\n    // cross-fade against it. uStage 0..4 builds the aura exponentially,\n    // 4..6 releases back down the same curve in one unbroken slope.\n    float s = uStage;\n    float sb = max(min(s, 4.0) - max(0.0, s - 4.0) * 2.0, 0.0);\n    float I = clamp((exp2(sb * 0.5) - 1.0) / 3.0, 0.0, 1.0);\n    // the organic hand-back starts deep into the release and only finishes\n    // once the next section fills the viewport\n    float fadeBack = clamp((s - 4.8) / 1.2, 0.0, 1.0);\n    // spatial clip: the aura blooms in from the section's side of the\n    // seam as it scrolls in, and wipes out with it on the way back up.\n    // A CONSTANT-width ramp (never a tightening band — that read as a\n    // hard cutout) dissolves into full coverage across the last half\n    // viewport of the approach, so by the time the section pins the\n    // gradient has already melted away instead of compressing into a\n    // line. The brief soft tint that crosses the seam mid-transition is\n    // the same gradual wash the release shows on the far side. uProcTop\n    // tracks the layout directly, so this holds at ANY scroll speed.\n    float doneness = 1.0 - clamp(uProcTop * 2.0, 0.0, 1.0);\n    float clipTop = mix(smoothstep(uProcTop, uProcTop + 0.6, uvV.y), 1.0, doneness);\n    float vis = uProcAmt * clipTop * (1.0 - meltMask(uvUp, time, fadeBack, 53.9));\n\n    // --- ink flood: the clicked word's soft-halo mask, liquefied by a\n    // drifting warp and cut by a threshold sweep that starts at the glyph\n    // cores and finishes past the softest halo — the word pours outward\n    // until it IS the field, then disperses the same way in reverse\n    float flood = 0.0;\n    if (uInkAmt > 0.003) {\n      vec2 iw = uvUp + dvec * 0.8;\n      iw += (vec2(ns(iw * 3.1 + vec2(time * 0.07, 0.0)),\n                  ns(iw * 3.1 - vec2(0.0, time * 0.05))) - 0.5)\n            * (0.02 + 0.10 * uInkAmt);\n      float inkA = texture2D(uInk, iw).a;\n      float nz2 = ns((uvUp + dvec) * vec2(aspect, 1.0) * 2.6\n                     + vec2(time * 0.06, -time * 0.04));\n      float edge0 = 0.82 - uInkAmt * 1.25;\n      flood = smoothstep(edge0, edge0 + 0.22, inkA + (nz2 - 0.5) * 0.28);\n    }\n\n    // --- color gradient riding the smoke trail -------------------------------\n    // Theme-split palettes, blended by drifting noise and revealed by the\n    // trail's intensity. Applied after inversion so it reads the same inside\n    // the headline glyphs. The trail inherits the field's grain (via f, which\n    // contains gr) and never fully replaces the background, so it feels like\n    // part of it rather than a layer on top.\n    float p1 = ns(uv * 3.0 + vec2(time * 0.1, 0.));\n    float p2 = ns(uv * 4.0 - vec2(0., time * 0.12));\n    float p3 = ns(uv * 2.5 + vec2(time * 0.07, -time * 0.05));\n\n    // light: purple base -> pink / yellow / green\n    vec3 vividL = vec3(0.545, 0.361, 0.965);\n    vividL = mix(vividL, vec3(0.925, 0.282, 0.600), smoothstep(0.25, 0.85, p1));\n    vividL = mix(vividL, vec3(0.980, 0.800, 0.082), smoothstep(0.35, 0.90, p2) * 0.8);\n    vividL = mix(vividL, vec3(0.133, 0.773, 0.420), smoothstep(0.40, 0.92, p3) * 0.7);\n\n    // dark: purple base -> dark blue / violet, faint yellow + green accents,\n    // pink core near the cursor head\n    vec3 vividD = vec3(0.486, 0.227, 0.929);\n    vividD = mix(vividD, vec3(0.118, 0.227, 0.541), smoothstep(0.30, 0.90, p2) * 0.85);\n    vividD = mix(vividD, vec3(0.545, 0.361, 0.965), smoothstep(0.35, 0.90, p3) * 0.6);\n    vividD = mix(vividD, vec3(0.980, 0.800, 0.082), smoothstep(0.55, 0.95, p1 * p3) * 0.25);\n    vividD = mix(vividD, vec3(0.133, 0.773, 0.420), smoothstep(0.50, 0.95, p2 * p3) * 0.25);\n    vividD = mix(vividD, vec3(0.925, 0.282, 0.600), smoothstep(0.45, 0.95, n.r) * 0.75);\n\n    vec3 vivid = mix(vividL, vividD, th);\n    vivid *= 0.8 + 0.45 * f;                       // field texture + grain in the trail\n    float colorAmt = smoothstep(0.04, 0.9, n.r + gr * 0.5);\n    colorAmt = min(colorAmt, 0.8);                 // background always shows through\n    // the aura mix below would cover this base-pass trail, so it fades\n    // out here and a lighter, grainier version of the SAME trail is\n    // re-laid on top of the aura inside the process block\n    float trailBase = colorAmt;\n    colorAmt *= 1.0 - max(vis, flood);\n    col = mix(col, vivid, colorAmt);\n\n    // --- the process material: holographic CMYK aura ------------------------\n    // One continuous field, generalized from the analog-grain holographic\n    // aura refs. Two full-spectrum sheets ride a domain-warped swirl; where\n    // the sheets disagree the mix greys out into the muted zones between\n    // ribbons, exactly like the references. Everything is graded by\n    // progress: uStage 0..4 walks the aura from a faint pastel wash —\n    // barely more than the site field, same grain — into full CMYK\n    // saturation with deep swirl distortion and thicker film grain, and\n    // 4..5 melts it back into the calm background along organic noise\n    // borders. Movement is SCROLL-DRIVEN: the flow phase and the swirl\n    // rotation ride uStage (rate-capped upstream), so every scroll trigger\n    // stirs and distorts the field, and at rest it only barely breathes.\n    // The cursor trail keeps displacing it throughout.\n    if (vis > 0.003 || flood > 0.003) {\n      // effective intensity: the scroll-driven curve, or — when a word is\n      // pouring — the flood forces the aura near its full-energy state so\n      // the ink arrives alive rather than as the faint first-pane wash\n      float Ie = max(I, uInkAmt * 0.9);\n      // the flow phase rides the SAME exponential curve, so scrolling stirs\n      // harder pane by pane; the ambient floor is deliberately high so the\n      // field is always visibly flowing, even parked on the first pane\n      float ph = Ie * 4.4 + time * (0.42 + 0.18 * Ie);\n      float zoom = 1.45 + 0.15 * Ie;\n      vec2 sp = (uvUp + dvec * (1.5 + 1.0 * Ie)) * vec2(aspect, 1.0) * zoom;\n      // swirl rotation about the viewport centre: scroll-driven, with a\n      // steady constant churn while static\n      float ang = Ie * 0.88 + time * 0.028;\n      vec2 ctr = vec2(aspect, 1.0) * zoom * 0.5;\n      vec2 rp = sp - ctr;\n      sp = ctr + mat2(cos(ang), -sin(ang), sin(ang), cos(ang)) * rp;\n\n      // domain warp deepens with progress, but even the first pane gets\n      // real curl and fine detail — the wash swirls, it doesn't sit flat\n      vec2 q = vec2(\n        ns(sp * 0.8 + vec2(ph * 0.14, 3.0)),\n        ns(sp * 0.8 - vec2(7.0, ph * 0.11)));\n      vec2 q2 = vec2(\n        ns(sp * 1.7 + q * 2.0 + vec2(11.0, ph * 0.07)),\n        ns(sp * 1.7 + q * 2.0 - vec2(ph * 0.06, 4.0)));\n      vec2 fp = sp + (q - 0.5) * (1.5 + 1.9 * Ie) + (q2 - 0.5) * (0.8 + 1.0 * Ie);\n\n      // two full-spectrum sheets (ns is biased dark — sqrt flattens); the\n      // wheel rotates a touch with the scroll so each pane lands on its\n      // own slice of the spectrum\n      float t1 = sqrt(ns(fp * 0.85));\n      float t2 = sqrt(ns(fp * 1.45 + 19.0));\n      vec3 w1 = 0.5 + 0.5 * cos(6.28318 * (0.02 + s * 0.05 + t1 * 0.9 + vec3(0.0, 0.33, 0.66)));\n      vec3 w2 = 0.5 + 0.5 * cos(6.28318 * (0.55 + s * 0.05 + t2 * 0.9 + vec3(0.0, 0.33, 0.66)));\n      vec3 aura = w1 * 0.62 + w2 * 0.38;\n      aura = mix(aura, vec3(1.0), 0.22);      // holographic milk\n\n      // progress grading: soft-but-present pastel wash -> full CMYK\n      float lum = dot(aura, vec3(0.299, 0.587, 0.114));\n      aura = mix(vec3(lum), aura, 0.45 + 0.55 * Ie);\n      aura = mix(aura, aura * vec3(0.40, 0.38, 0.52) + vec3(0.03), th);\n\n      // analog film grain, per-channel so it sparkles slightly: reads like\n      // the site field at the start and thickens as the aura takes over\n      float g2 = mix(-grAmp, grAmp, rand(uv * 1.31 + 4.7 + sin(time)));\n      aura += vec3(gr, mix(gr, g2, 0.5), g2) * (0.20 + 0.14 * Ie);\n\n      if (uInverse > 0.5) aura = vec3(1.0) - aura;\n      col = mix(col, aura, max(vis * (0.42 + 0.58 * Ie), flood));\n\n      // --- the standard site cursor, re-laid over the aura: the same\n      // vivid trail as everywhere else, just lighter and with extra film\n      // grain worked into it so it melts into the analog field. Applied\n      // after the inversion, like the base pass, so it reads the same\n      // inside the headline glyphs.\n      col = mix(col, vivid + gr * 0.55, trailBase * 0.55 * max(vis, flood));\n    }\n\n    // glyph mask sampled at the SAME displaced uv, so the smoke trail melts\n    // the letterforms exactly like the reference warps its media planes\n    float alpha = 1.0;\n    if (uHasMask > 0.5) {\n      vec2 mUv = vUv + dvec * (uViewport / uSize);\n      alpha = texture2D(uMask, mUv).a;\n    }\n\n    gl_FragColor = vec4(col, alpha);\n  }\n",uniforms:C,transparent:!0}));P.frustumCulled=!1,D.add(P);let z=()=>{let e=p.getBoundingClientRect(),t=Math.max(e.width,1),n=Math.max(e.height,1);v.setSize(t,n,!1),C.uSize.value.set(t,n);let a=window.innerWidth,i=window.innerHeight;C.uViewport.value.set(a,i),w.setSize(Math.ceil(a/3),Math.ceil(i/3)),g.left=-a/2,g.right=a/2,g.top=i/2,g.bottom=-i/2,g.updateProjectionMatrix()};z();let R=new ResizeObserver(z);R.observe(p),window.addEventListener("resize",z);let L=!0,E=new IntersectionObserver(e=>{var t,n;L=null==(n=null==(t=e[0])?void 0:t.isIntersecting)||n});E.observe(p);let q=0,O=-1,V=null,B=-1,Y=null,F=!h.current.inverse&&!h.current.fragment&&!u,G=0,H="",N=new Uint8Array(4),_=e=>{if(q=requestAnimationFrame(_),(0,o.fl)(e),!L)return;let t=null==u?void 0:u.current;t&&t.version!==O&&(O=t.version,null==V||V.dispose(),V=new r.GOR(t.canvas),C.uMask.value=V,C.uHasMask.value=1);let n=(0,o.nN)();for(let e=0;e<o.lH;e++){let t=n[e];t.visible?(M.position.set(t.x,t.y,0),M.rotation.z=t.rot,M.scale.set(t.scale,t.scale,1)):(M.position.set(0,0,0),M.rotation.z=0,M.scale.set(0,0,0)),M.updateMatrix(),b.setMatrixAt(e,M.matrix);let a=Math.min(t.opacity/.22,1);y.setXY(e,t.visible?t.opacity:0,a*a)}b.instanceMatrix.needsUpdate=!0,y.needsUpdate=!0;let a=(0,o.UW)(),i=1+Math.min(.04*a.speed,1.7),d=190*(i-1)*.4;T.position.set(a.x-Math.cos(a.angle)*d,a.y-Math.sin(a.angle)*d,0),T.rotation.z=a.angle,T.scale.set(190*i,190/(1+(i-1)*.4),1);let x=(0,c.rP)(),k=p.getBoundingClientRect();C.uOffset.value.set(k.left,k.top),C.uTime.value=(0,o.Hl)(),C.uClock.value=e/1e3,C.uThemePrev.value=x.prev,C.uThemeTarget.value=x.target,C.uRippleT0.value=x.rippleT0,C.uRippleCenter.value.set(x.center.x,x.center.y);let A=(0,s.X)();C.uStage.value=A.stage,C.uProcAmt.value=A.amt,C.uProcTop.value=A.top;let S=(0,l.rf)();if(S.version!==B&&(B=S.version,null==Y||Y.dispose(),Y=S.canvas?new r.GOR(S.canvas):null,C.uInk.value=null!=Y?Y:I),C.uInkAmt.value=S.amt,C.uInverse.value=+!!h.current.inverse,v.setRenderTarget(w),v.render(f,g),v.setRenderTarget(null),v.render(D,U),F&&(G-=1)<=0){G=24;let e=v.getContext(),t=Math.floor(e.drawingBufferWidth/2),n=Math.max(0,e.drawingBufferHeight-Math.ceil(6*m));if(e.readPixels(t,n,1,1,e.RGBA,e.UNSIGNED_BYTE,N),N[3]>0){let e=N[0],t=N[1],n=N[2],a=(.2126*e+.7152*t+.0722*n)/255;if(a>.8){let i=.8/a;e=Math.round(e*i),t=Math.round(t*i),n=Math.round(n*i)}let i="#".concat((0x1000000|e<<16|t<<8|n).toString(16).slice(1));if(i!==H){H=i;let e=document.querySelector('meta[name="theme-color"]');e&&(e.content=i),document.body.style.backgroundColor=i}}}};return q=requestAnimationFrame(_),()=>{cancelAnimationFrame(q),R.disconnect(),E.disconnect(),window.removeEventListener("resize",z),null==V||V.dispose(),null==Y||Y.dispose(),I.dispose(),k.dispose(),x.dispose(),A.dispose(),S.dispose(),w.dispose(),P.geometry.dispose(),P.material.dispose(),v.dispose()}},[e]),v}},9245:(e,t,n)=>{n.d(t,{GO:()=>r,Yl:()=>o,rP:()=>s});let a={prev:0,target:0,rippleT0:-1e4,center:{x:.5,y:.5}},i=!1;function r(){if(i||"undefined"==typeof document)return;i=!0;let e=+("dark"===document.documentElement.dataset.theme);a.prev=e,a.target=e}function o(e){let t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:.5,n=arguments.length>2&&void 0!==arguments[2]?arguments[2]:.5,i=!(arguments.length>3)||void 0===arguments[3]||arguments[3];r();let o=+("dark"===e);if(o===a.target)return;a.prev=a.target,a.target=o,a.rippleT0=performance.now()/1e3,a.center={x:t,y:n},document.documentElement.dataset.theme=e;let s=document.querySelector('meta[name="theme-color"]');if(s&&(s.content="dark"===e?"#0C0B10":"#CBC5D5"),i)try{localStorage.setItem("theme",e)}catch(e){}}function s(){return a}}}]);